{% extends 'base.html.twig' %}

{% block title %}Informe parcial del periodo {{ estudiante.periodo }}{% endblock %}

{% block body %}


<div class="container d-flex justify-content-center">
    <div class="row d-flex justify-content-center m-2 p-5">

        <div class="row d-flex justify-content-center m-5 p-5"><h2>Informe parcial del periodo {{ estudiante.periodo }} - Sede {{ estudiante.sede|title }} 2024</h2></div>
        <h3>{{ estudiante.nombre|title }} - Grado: {{ estudiante.grado|title }}</h3>
        <h3>Cantidad de materias reportadas: {{ cantidadMateriasReportadas }}</h3>

        <ul>
            {% if estudiante.uno %} <li>{{ estudiante.uno }}</li>{% endif %}
            {% if estudiante.dos %} <li>{{ estudiante.dos }}</li>{% endif %}
            {% if estudiante.tres %} <li>{{ estudiante.tres }}</li>{% endif %}
            {% if estudiante.cuatro %} <li>{{ estudiante.cuatro }}</li>{% endif %}
            {% if estudiante.cinco %} <li>{{ estudiante.cinco }}</li>{% endif %}
            {% if estudiante.seis %} <li>{{ estudiante.seis }}</li>{% endif %}
            {% if estudiante.siete %} <li>{{ estudiante.siete }}</li>{% endif %}
            {% if estudiante.ocho %} <li>{{ estudiante.ocho }}</li>{% endif %}
            {% if estudiante.nueve %} <li>{{ estudiante.nueve }}</li>{% endif %}
            {% if estudiante.diez %} <li>{{ estudiante.diez }}</li>{% endif %}
            {% if estudiante.once %} <li>{{ estudiante.once }}</li>{% endif %}
            {% if estudiante.doce %} <li>{{ estudiante.doce }}</li>{% endif %}
            {% if estudiante.trece %} <li>{{ estudiante.trece }}</li>{% endif %}
            {% if estudiante.catorce %} <li>{{ estudiante.catorce }}</li>{% endif %}
        </ul>

        {% if cantidadMateriasReportadas > 0 %} 
        
            <h5>
                El estudiante {{ estudiante.nombre|title }} debe presentarse con su acudiente a cada uno de los profesores de las materias reportadas 
                el {{ estudiante.fecha }} a las {{ estudiante.hora }} en la sede {{ estudiante.lugar }}. 
            </h5>
            <p>Este informe hace parte del seguimiento académico del estudiante.</p>
            <h5 style="color: red;"> {{ estudiante.observaciones }}</h5>
        {% endif %}

        {% if cantidadMateriasReportadas == 0 %}
            <h3 style="color: green;">
                ¡FELICITACIONES!🥳, sigue aprendiendo con esfuerzo y voluntad para que No repruebes ninguna materia en el periodo {{ estudiante.periodo }}
            </h3>

        {% endif %}
        
        <a href="{{ path('app_inicio') }}" class="btn btn-primary">Regresar </a>
    </div>
    
</div>
{% endblock %}
